import 'package:flutter/material.dart';

class ColorPalette {
  static const primary = Color.fromRGBO(44, 61, 112, 1);
  static const disable = Colors.grey;
  static const black = Colors.black;
  static const blackBackgound = Colors.black54;
  static const white = Colors.white;
  static const whiteBackground = Colors.white30;
}